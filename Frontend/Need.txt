Frontend:

Modals for: stats, scores/leaderboard, creating new games and editing existing ones

*Check with Claude or GPT if I need these files

GameApi 

GameSchema -->

GameForm --> handle all states --> selections such as difficulty and keeps track mistakes and hints used
         --> call GameSchema
         --> use yup:
             import { useForm } from "react-hook-form";
             import { yupResolver } from "@hookform/resolvers/yup";

Start with one file if I need to
Refer to layers comment from mentor Dan

GameContext --> context layer --> imports useGame & gameContext --> refer to useContext themes project

GameService --> hook layer --> state + side effects
calls game api and fetches CRUD that can be passed to other files such as useGames.js

UseGames.js -->  service layer (API only) --> CRUD calls
final frontend CRUD that calls gameService (no need to do fetching again!)